动量法：
  一种更新：选择用泄露平均值来代替梯度的计算；将瞬时梯度替换为多个“过去”梯度的平均值。
  给一个例子：
    如果一个函数在x2上变化较为震荡，而在x1上变化较为缓慢，那么我们可以直觉到计算过去的平均梯度效果会很好。
  毕竟，在x1方向上，这将聚合非常对齐的梯度，从而增加我们在每一步中覆盖的距离。相反，在梯度震荡的x2方向，
  由于互相抵消了对方的震荡，聚合梯度将减小步长的大小。

    
