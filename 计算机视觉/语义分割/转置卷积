转置卷积：
  卷积不会增大输入的高宽，通常要么不变，要么减半
  转置卷积则可以用来增大输入高宽



  Y[i : i+h, j : j+w] += X[i.j] * K


为什么称之为转置？
  对于卷积 Y=X*W，（这里的*代表卷积）
    可以对W构造一个V，使得卷积等价于矩阵乘法Y‘ = VX’
    这里Y'.X'是Y,X对应的向量版本
  转置卷积则等价于Y'=(V^T)X'（同样是把W构造为V，但此时V应该满足之前那个等式）
  如果卷积将输入从(h,w)变成了(h',w')
    同样超参数的转置卷积则从(h',w')变成(h,w)


注意：转置卷积不会还原卷积操作中的原始像素值或特征值，它仅仅通过一种“结构上的逆过程”来：
      把尺寸从小恢复到大，并重新生成新的特征图。
  
