一类目标检测算法是基于锚框：
  提出多个被称为锚框的区域(边缘框)
  预测每个锚框是否含有关注的物体
  如果是，预测从这个锚框到真实边缘框的偏移

IoU-交并比：
  IoU用来计算两个框的相似度
    0表示无重叠，1表示完全重叠
  Jacquard指数的计算

赋予锚框标号：
  每个锚框是一个训练样本
  将每个锚框，要么标注乘背景，要么关联上一个真实边缘框(到这个边缘框的偏移)
    一张图片可能生成上万或者上百万个锚框，真实边缘框不多，可能就10个，于一个锚框而讲，绝大部分锚框都
    只是一个背景--->导致大量的负类样本
  我们可能会生成大量锚框：
    这个导致大量的负类样本


锚框是根据每次输入进来的图片生成的，边缘框是提前标好的；
赋予锚框标号这个过程是每次图片输入后要做的一个过程
一个图片输入后可能会生成许多个训练样本。赋予标号的算法有很多种
